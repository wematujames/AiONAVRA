import { StyleSheet, Text, View } from "react-native";
import React from "react";

const CreateAppointmentScreen = () => {
  return (
    <View>
      <Text>CreateAppointmentScreen</Text>
    </View>
  );
};

export default CreateAppointmentScreen;

const styles = StyleSheet.create({});
