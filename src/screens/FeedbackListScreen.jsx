import { SafeAreaView, StyleSheet, Text } from "react-native";
import React from "react";

const FeedbackListScreen = () => {
  return (
    <SafeAreaView>
      <Text>FeedbackListScreen</Text>
    </SafeAreaView>
  );
};

export default FeedbackListScreen;

const styles = StyleSheet.create({});
